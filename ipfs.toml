# eris service definition for tendermint
name           = "ipfs"
image          = "eris/ipfs"
data_container = true
ports          = [ "4001:4001", "5001", "8080" ]
volumes        = [ "$eris/files/staging:/export:ro" ]
environment    = [  ]

[maintainer]
name = "Eris Industries"
email = "support@erisindustries.com"

[location]
repository = "github.com/eris-ltd/eris-services"

[machine]
include = [ "docker" ]
requires = ""
